---
'@signozhq/design-tokens': minor
---

Add the surface scale (`surface-1`, `surface-2`, `surface-3`, `surface-static`) and an `l3-background-30` layer tint. The 60 variants of the layer backgrounds now mix 20% transparency instead of 40%, and the primary callout icon, title and description were retuned in both themes.
