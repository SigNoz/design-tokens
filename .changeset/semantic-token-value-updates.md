---
'@signozhq/design-tokens': patch
---

Point the secondary background, foreground and border at the layer tokens instead of raw neutral ramps, so they follow the surface scale. Hover backgrounds for primary, warning and danger now darken (600) rather than lighten (400), and the light warning background moves to amber 500.
